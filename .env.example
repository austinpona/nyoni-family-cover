# Public canonical website URL, without a trailing slash
NEXT_PUBLIC_SITE_URL=https://www.example.com

# Google Search Console verification token, from Search Console's "HTML tag"
# method — the content="..." value only, not the whole tag. Not a secret; it
# only proves control of the domain. Leave unset and nothing is rendered.
NEXT_PUBLIC_GOOGLE_SITE_VERIFICATION=
